import * as React from "react"
import { NEARAuthRoute } from "../../components/nearAuth"

export default function() {
    return <NEARAuthRoute>
        You are authenticated to NEAR
    </NEARAuthRoute>
}